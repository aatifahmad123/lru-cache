#include<iostream>
using namespace std;

class LRUCache{
    int cap; // capacity
    int curr; // current size

public:
    LRUCache(int capacity) {
        cap = capacity;
        curr = 0;
    }
};

int main() {

    // your code goes here

    return 0;
}